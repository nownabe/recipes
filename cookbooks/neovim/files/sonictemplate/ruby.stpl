\(\S\+\)\.atcoder$
	#
	
	if ENV["ATCODER_TEST"] == "true"
	  require "./base"
	else
	  Base = Class.new { def self.add_sample(*args); end }
	end
	
	class {{$1}} < Base
	  add_sample(
	    input: [{{_cursor_}}],
	    expect: [],
	  )
	
	  attr_reader
	
	  def initialize
	  end
	
	  def run
	  end
	
	  def solve
	  end
	end
	
	{{$1}}.new.run
